How to Run
There is no main in the program, all that was programmed were the class constructors. Expected use of the classes are to use them to create objects then draw them. A program can invoke the classes by creating new objects of that type.

Short Response
Answer the following questions:
1. Interfaces have only abstract methods, and abstract methods have a mixture of implemented and abstract methods.

Implements vs Extends keyword, abstract classes is a mixture of implementation and interface, while an interface is pure inheritance of interface.

2. They both use is-a relationships.
They both define a new type.

3. gvim -p *.java
